{
  "buildCommand": "npx prisma generate && if [ \"$VERCEL_ENV\" = \"production\" ]; then npx prisma migrate deploy; fi && npm run build:with-screenshots",
  "crons": [
    {
      "path": "/api/crons/sendDailyQuote",
      "schedule": "0 15 * * *"
    },
    {
      "path": "/api/crons/sendContinueReadingAfter24Hours",
      "schedule": "0 15 * * *"
    }
  ]
}
